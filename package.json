{
  "name": "tubeapp",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm rum build --target=production --aot --prefix frontend && npm install --prefix backend "
  }
}